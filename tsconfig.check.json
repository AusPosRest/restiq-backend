{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // nothing is emitted here, so the build's src-only rootDir does not apply
    "rootDir": "."
  },
  "include": ["src", "test", "scripts", "prisma.config.ts", "vitest.config.ts", "vitest.e2e.config.ts"],
  "exclude": []
}
